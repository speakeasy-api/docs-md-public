import type { CSSProperties } from "react";

export type CopyIconProps = {
  /**
   * Extra classes to apply to the icon
   */
  className?: string;
  /**
   * Extra styles to apply to the icon
   */
  style?: CSSProperties;
};
